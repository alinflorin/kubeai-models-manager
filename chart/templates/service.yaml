apiVersion: v1
kind: Service
metadata:
  name: {{ include "kmm.fullname" . }}
  labels:
    {{- include "kmm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kmm.selectorLabels" . | nindent 4 }}
