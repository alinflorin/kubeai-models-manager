name: kmm-ui Docker CI

on:
  push:
    branches:
      - main
    paths: 
      - src/kmm-ui/**

  pull_request:
    branches:
      - main
    paths: 
      - src/kmm-ui/**

jobs:
  reusable-docker:
    permissions:
      actions: read
      contents: read
      packages: write
    uses: ./.github/workflows/reusable-docker-ci.yaml
    with:
      project: 'kmm-ui'
      push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
      scan: true
      version: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && 'latest' || github.sha }}
    secrets: inherit